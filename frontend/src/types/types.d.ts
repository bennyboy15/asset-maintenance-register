export type AssetType = {
    id: string;
    name: string;
}

export type SupplierType = {
    name: string
}

export type AssetGroupType = {
    name: string
}

export type UserType = {
    name: string;
    email: string;
    created_at: date;
    updated_at: date;
}